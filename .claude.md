# WoW Character Manager - 项目概览

## 架构原则

**所有游戏数据来自插件导入或网络爬取，页面不提供手动创建入口。**
例外：用户个人数据（装备需求清单 item_needs、天赋配点方案 talent_builds、系统设置 settings）允许手动 CRUD。

## 技术栈

### 后端
- **语言**: Python 3.12
- **框架**: FastAPI
- **数据库**: SQLite (aiosqlite 异步驱动)
- **数据验证**: Pydantic v2

### 前端
- **框架**: Vue 3.3.4 (Composition API)
- **语言**: TypeScript 5.2.2
- **构建工具**: Vite 5.0.0
- **UI组件**: Element Plus 2.4.2
- **状态管理**: Pinia 2.1.7
- **图表**: ECharts 6.0.0

## 数据流架构

| 数据 | 来源 | 导入方式 | 页面可编辑 |
|------|------|----------|-----------|
| 副本/Boss/掉落 | AtlasLootMY 插件 | /api/dungeons/import-atlasloot | 否 |
| 角色（等级/职业/种族） | tdInspect 插件 | /api/character-refresh/refresh-all | 仅添加，不可编辑同步字段 |
| 装备 | tdInspect / 暴雪API | character_equipment 表持久化 | 否 |
| 套装 | 装备导入自动解析 | character_item_sets 表 | 否 |
| 金币 | Accountant 插件 | /api/gold/refresh | 否 |
| 物品 | AtlasLoot / WoWDataExporter | 导入脚本 | 否 |
| 天赋树/节点 | 数据网站 | import_talents.py | 否 |
| 装备需求清单 | 用户手动 | 页面表单 | 是 |
| 天赋配点方案 | 用户手动 | 页面表单 | 是 |
| 系统设置 | 用户手动 | /api/settings | 是 |

## 数据库表 (SQLite, 15 张)

characters, items, dungeons, bosses, item_needs, boss_loot,
character_gold, gold_transaction, gold_snapshot,
character_equipment, character_item_sets,
talent_trees, talent_nodes, talent_builds, settings

## 项目结构
```
wow-character-manager/
├── backend/
│   ├── app/
│   │   ├── api/           # API路由 (characters, dungeons, bosses, gold, equipment, talents, settings, etc.)
│   │   ├── core/          # 核心配置 (config.py, database.py)
│   │   └── models/        # Pydantic 模型 (talent_models.py)
│   ├── main.py            # FastAPI 入口
│   ├── import_atlasloot_my_fixed.py  # AtlasLoot 导入
│   ├── import_tdinspect.py           # tdInspect 解析
│   ├── import_accountant.py          # Accountant 导入
│   ├── import_equipment_data.py      # 装备数据导入
│   ├── import_talents.py             # 天赋数据导入
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── api/           # API调用封装
│   │   ├── views/         # 页面组件
│   │   ├── stores/        # Pinia 状态管理
│   │   ├── types/         # TypeScript 类型定义
│   │   └── utils/         # 工具函数
│   └── package.json
└── README.md
```

## 关键约定

- 数据库使用 `app.core.database.db` 单例 (aiosqlite)，不使用 ORM
- settings.py 使用同步 sqlite3（历史遗留），其余 API 均使用异步 db
- 装备数据优先从 character_equipment 表读取，回退到 characters.equips_data
- 前端 API 基础路径通过 Vite proxy 转发到后端
