# WoW Character Manager - 项目概览

## 项目简介
魔兽世界角色管理系统是一个用于管理魔兽世界游戏角色、副本进度、Boss信息和装备需求的现代化Web应用。

## 技术架构
采用前后端分离架构，分别开发独立部署。

### 后端技术栈
- **语言**: Python 3.8+
- **框架**: FastAPI 0.136.1
- **数据库**: MongoDB (Motor异步驱动)
- **API集成**: 暴雪游戏数据API
- **认证**: JWT (python-jose)
- **数据验证**: Pydantic 2.13.3

### 前端技术栈
- **框架**: Vue 3.3.4 (Composition API)
- **语言**: TypeScript 5.2.2
- **构建工具**: Vite 5.0.0
- **UI组件**: Element Plus 2.4.2
- **状态管理**: Pinia 2.1.7
- **路由**: Vue Router 4.2.5
- **HTTP客户端**: Axios 1.6.0

## 项目结构
```
wow-character-manager/
├── backend/                 # 后端服务
│   ├── app/
│   │   ├── api/            # API路由层
│   │   ├── core/           # 核心配置
│   │   ├── models/         # 数据模型
│   │   ├── schemas/        # 数据验证
│   │   ├── services/       # 业务逻辑
│   │   └── utils/          # 工具函数
│   ├── main.py             # 应用入口
│   ├── requirements.txt    # Python依赖
│   └── .env.example        # 环境变量模板
├── frontend/               # 前端应用
│   ├── src/
│   │   ├── api/           # API调用封装
│   │   ├── assets/        # 静态资源
│   │   ├── components/    # 可复用组件
│   │   ├── router/        # 路由配置
│   │   ├── stores/        # 状态管理
│   │   ├── types/         # 类型定义
│   │   ├── views/         # 页面组件
│   │   ├── App.vue        # 根组件
│   │   └── main.ts        # 应用入口
│   ├── public/            # 公共资源
│   ├── package.json       # 依赖配置
│   └── vite.config.ts     # 构建配置
├── start.bat              # Windows启动脚本
├── start.sh               # Linux/Mac启动脚本
├── README.md              # 项目说明
├── SETUP.md               # 详细配置指南
├── QUICKSTART.md          # 快速开始指南
└── INSTALLATION_COMPLETE.md # 安装完成报告
```

## 核心功能

### 1. 角色管理
- 添加、编辑、删除角色信息
- 支持多个魔兽世界角色
- 记录职业、专精、等级、阵营等信息
- 职业标签颜色区分和阵营标识

### 2. 副本管理
- 管理副本信息（名称、难度、最低等级等）
- 从暴雪API同步副本数据
- 支持多种难度模式（普通、英雄、史诗等）

### 3. Boss管理
- 管理Boss信息和掉落
- 从暴雪API同步Boss数据
- 关联Boss到对应副本

### 4. 装备需求管理
- 为角色添加需要的装备
- 记录装备来源（副本、Boss）
- 设置装备优先级（1-5星）
- 标记装备获取状态
- 可视化展示获取进度

### 5. 暴雪API集成
- 自动获取装备图标和属性
- 获取副本和Boss的官方信息
- 支持中英文属性映射
- 智能缓存和错误处理

## 开发环境要求

### 后端环境
- Python 3.8+
- MongoDB 4.4+
- pip包管理器

### 前端环境
- Node.js 16+
- npm或yarn包管理器

### 可选环境
- 暴雪API开发者账号（用于同步游戏数据）

## 快速开始

### 1. 安装依赖
```bash
# 后端
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt

# 前端
cd frontend
npm install
```

### 2. 配置环境
```bash
cd backend
cp .env.example .env
# 编辑.env文件，配置MongoDB和暴雪API凭证
```

### 3. 启动服务
```bash
# 使用启动脚本
./start.sh  # Linux/Mac
start.bat   # Windows

# 或手动启动
# 后端
cd backend
python -m uvicorn main:app --reload

# 前端
cd frontend
npm run dev
```

### 4. 访问应用
- 前端界面: http://localhost:5173
- 后端API文档: http://localhost:8000/docs

## 开发规范

### 代码风格
- **后端**: 遵循PEP 8规范，使用类型注解
- **前端**: 遵循Vue 3风格指南，使用TypeScript严格模式

### 命名规范
- **文件名**: kebab-case (如: user-service.ts)
- **组件名**: PascalCase (如: UserService)
- **变量名**: camelCase (如: userName)
- **常量名**: UPPER_SNAKE_CASE (如: API_BASE_URL)

### Git提交规范
```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式调整
refactor: 重构代码
test: 测试相关
chore: 构建/工具链相关
```

## 部署说明

### 后端部署
1. 安装Python和MongoDB
2. 配置生产环境变量
3. 使用gunicorn或uvicorn部署
4. 配置nginx反向代理

### 前端部署
1. 构建生产版本: `npm run build`
2. 部署dist目录到Web服务器
3. 配置nginx或Apache

## 性能优化

### 后端优化
- 异步IO处理数据库操作
- API响应缓存
- 数据库查询优化
- 连接池管理

### 前端优化
- 路由懒加载
- 组件按需引入
- 图片懒加载和压缩
- 代码分割和压缩

## 安全考虑

### 认证和授权
- JWT token认证（计划实现）
- 基于角色的访问控制（计划实现）
- API请求频率限制
- 输入验证和SQL注入防护

### 数据安全
- 敏感信息加密存储
- HTTPS通信
- CORS配置
- 安全的HTTP头

## 测试策略

### 后端测试
- 单元测试（pytest）
- API集成测试
- 数据库测试

### 前端测试
- 组件单元测试（Vitest）
- E2E测试（Playwright）
- 类型检查（TypeScript）

## 贡献指南

1. Fork项目
2. 创建功能分支
3. 提交更改
4. 推送到分支
5. 创建Pull Request

## 许可证
MIT License

## 联系方式
- 项目地址: [GitHub Repository]
- 问题反馈: [Issues]
- 文档: [Wiki]

## 更新日志
- v1.0.0 (2024-05-06): 初始版本发布
  - 完整的角色管理功能
  - 装备需求追踪系统
  - 副本和Boss管理
  - 暴雪API集成
  - 响应式前端界面

## 未来计划
- [ ] 用户认证和权限管理
- [ ] 多语言支持（国际化）
- [ ] 数据导出功能（Excel/PDF）
- [ ] 装备对比工具
- [ ] 团队协作功能
- [ ] 移动端应用
- [ ] 数据分析和统计图表
- [ ] 实时通知系统