 找到了，"喵影掉落"就是 AtlasLootMY 系列（AtlasLoot 喵影版）。这个插件的数据是直接写在 Lua
  文件里的，不需要在游戏内操作，我可以直接读取。

  Read 2 files (ctrl+o to expand)

● 数据结构是 [itemID] = {[1]=bossNum, [2]=difficulty, [3]=1,
  [5]=quality}，但这个文件只有物品到Boss的映射，副本和Boss的名称定义应该在别的文件里。让我看看核心数据文件：

  Searched for 1 pattern, read 2 files (ctrl+o to expand)

● 数据非常完整！AtlasLootMY 的数据结构是：

  - data["副本ID"] = { name = "副本名", ... }
  - 每个副本下有多个 Boss 块，包含掉落物品ID

  可以完全从插件本地文件解析，不需要进游戏。让我看一下物品名称和本地化文件：


从这个插件里获取副本  boss  装备掉落等信息